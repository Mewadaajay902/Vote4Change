name: Welcome Message

on:
  push:
    branches:
      - master

jobs:
  display-message:
    runs-on: ubuntu-latest

    steps:
    - name: Print Welcome Message
      run: echo "welcome ajay mewada on github action"
